{
    "_comment": "MCP Server Configuration Template for Claude Code / Claude Desktop",
    "_usage": "Copy relevant sections to ~/.claude/claude_desktop_config.json or project .claude/settings.json",
    "mcpServers": {
        "filesystem": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-filesystem",
                "/path/to/your/project"
            ],
            "description": "File system operations — read, write, list, search files",
            "required": true
        },
        "puppeteer": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-puppeteer"
            ],
            "description": "Browser automation for E2E testing — navigate, click, type, screenshot",
            "required": true
        },
        "git": {
            "command": "uvx",
            "args": [
                "mcp-server-git",
                "--repository",
                "/path/to/your/project"
            ],
            "description": "Git operations — status, diff, log, commit, branch",
            "required": true
        },
        "github": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-github"
            ],
            "env": {
                "GITHUB_PERSONAL_ACCESS_TOKEN": "<your-github-pat>"
            },
            "description": "GitHub API — create issues, PRs, manage repos",
            "required": false
        },
        "sequential-thinking": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-sequential-thinking"
            ],
            "description": "Sequential reasoning — structured step-by-step thinking",
            "required": false
        },
        "fetch": {
            "command": "uvx",
            "args": [
                "mcp-server-fetch"
            ],
            "description": "HTTP fetch — retrieve web content for research",
            "required": false
        }
    },
    "setup_instructions": {
        "step_1": "Install Node.js 18+ and npm",
        "step_2": "Install uv (Python package manager): curl -LsSf https://astral.sh/uv/install.sh | sh",
        "step_3": "Copy mcpServers config to your Claude settings file",
        "step_4": "Replace /path/to/your/project with your actual project path",
        "step_5": "For GitHub MCP: create a personal access token at github.com/settings/tokens",
        "step_6": "Restart Claude Code / Claude Desktop to load new MCP servers"
    }
}